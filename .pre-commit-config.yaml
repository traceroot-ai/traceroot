ci:
  autofix_prs: true
  autoupdate_commit_msg: '[pre-commit.ci] pre-commit suggestions'
  autoupdate_schedule: monthly

# Stop after the first failing hook. Otherwise `mixed-line-ending` can rewrite the
# working tree and a later `lint-staged` pass still sees the old index → restore conflict.
fail_fast: true

repos:
  # General file hygiene
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: no-commit-to-branch
        args: [--branch, main]
      - id: end-of-file-fixer
      - id: mixed-line-ending
        args: [--fix, lf]
      - id: trailing-whitespace
      - id: check-toml
      - id: check-yaml
        exclude: ^deploy/helm/templates/

  # Python: Ruff (lint + format)
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.0
    hooks:
      - id: ruff
        args: [--fix, --exit-non-zero-on-fix]
      - id: ruff-format

  # Frontend: lint-staged for ESLint + Prettier
  - repo: local
    hooks:
      - id: frontend-lint
        name: frontend lint-staged
        entry: bash -c 'cd frontend && [ -f node_modules/.bin/lint-staged ] || pnpm install --frozen-lockfile && pnpm exec lint-staged'
        language: system
        files: ^frontend/
        pass_filenames: false
