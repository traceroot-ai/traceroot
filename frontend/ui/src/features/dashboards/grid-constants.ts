// The dashboard grid's fixed geometry, shared by everything that must agree
// with it: the grid itself, the widget auto-placement, and the assistant
// panel's dashboard miniature. One definition, so those surfaces cannot
// drift apart tile by tile.
export const COLS = 12;
export const ROW_HEIGHT = 56;
