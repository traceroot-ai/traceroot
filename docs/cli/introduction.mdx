---
title: Introduction
description: Read and export your traces from the terminal
---

The TraceRoot CLI (`traceroot`) is a small client for the TraceRoot public API. Authenticate once with your project API key, then list, inspect, and export traces directly from your terminal — no web app required.

As of V1, the CLI is read-only: you get readable span trees, durations, and errors inline — or JSON output for piping into scripts.

## Where to start

<CardGroup cols={2}>
  <Card title="Get Started" icon="rocket" href="/cli/get-started">
    Install the CLI, authenticate, and inspect your first trace in minutes.
  </Card>
  <Card title="Commands" icon="terminal" href="/cli/commands">
    Full reference for `login`, `status`, and the `traces` commands.
  </Card>
</CardGroup>
