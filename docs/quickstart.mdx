---
title: "Quickstart"
description: "Start building Python programs with awesome debugging and tracing in 3 minutes"
---

## Setup

### Register and Login

At first go to [https://auth.traceroot.ai/](https://auth.traceroot.ai/) to register or login.

If you don't have an account, you can register by clicking the **Sign Up** button:

<img
  src="https://raw.githubusercontent.com/traceroot-ai/traceroot/refs/heads/main/misc/images/docs/quickstart/0_signup.png"
  alt="Register"
  style={{ display: "block", margin: "0 auto" }}
/>

Then you can login with your registered email and password.

<img
  src="https://raw.githubusercontent.com/traceroot-ai/traceroot/refs/heads/main/misc/images/docs/quickstart/1_signin.png"
  alt="Signin"
  style={{ display: "block", margin: "0 auto" }}
/>

Then you can see the following page which means you have successfully logged in :)

<img
  src="https://raw.githubusercontent.com/traceroot-ai/traceroot/refs/heads/main/misc/images/docs/quickstart/2_logged_in.png"
  alt="Loggedin"
  style={{ display: "block", margin: "0 auto" }}
/>

### Get Token

You can get the token by navigating to the **Connect** page.

<img
  src="https://raw.githubusercontent.com/traceroot-ai/traceroot/refs/heads/main/misc/images/docs/quickstart/3_token.png"
  alt="Token"
  style={{ display: "block", margin: "0 auto" }}
/>

### Install SDK

Then you can install the [TraceRoot.AI](http://traceroot.ai) SDK in your Python package by

```bash
pip install traceroot==0.0.5
```

## Configure

### Environment Setup

To run any of the examples in this folder, you can configure Traceroot using environment variables.

---

### Option 1: Create a `.env` file in the root of your project

```bash
# Open the .env file and update it with your actual Traceroot token and details
TRACEROOT_TOKEN=your-actual-traceroot-token
TRACEROOT_SERVICE_NAME=example
TRACEROOT_GITHUB_OWNER=traceroot-ai
TRACEROOT_GITHUB_REPO_NAME=traceroot
TRACEROOT_GITHUB_COMMIT_HASH=main
TRACEROOT_LOCAL_MODE=true
```

Then run your program:

```bash
python file_name.py
```

---

### Option 2: Manually export the environment variables

```bash
export TRACEROOT_TOKEN=your-actual-traceroot-token
export TRACEROOT_SERVICE_NAME=example
export TRACEROOT_GITHUB_OWNER=traceroot-ai
export TRACEROOT_GITHUB_REPO_NAME=traceroot
export TRACEROOT_GITHUB_COMMIT_HASH=main
export TRACEROOT_LOCAL_MODE=true
```

Then run your program:

```bash
python file_name.py
```

---

---

✅ **Notes:**

- Only `TRACEROOT_TOKEN` and `TRACEROOT_SERVICE_NAME` are required.
- GitHub-related variables (`OWNER`, `REPO_NAME`, `COMMIT_HASH`) are optional.
- `TRACEROOT_LOCAL_MODE` can be set to `true` for local testing.

Then you can start tracing and logging by

```python
logger = traceroot.get_logger()

@traceroot.trace()
def main():
    logger.info("This is an info message")
```

And run your program:

```bash
python your_program.py
```

All of the logging and tracing will be automatically tracked and pushed to [TraceRoot.AI](http://traceroot.ai) (demo) platform.

### A Simple Example

[TraceRoot.AI](http://traceroot.ai) has been integrated with famous Python libraries like [camel](https://github.com/camel-ai/camel).

You can easily track the AI agent's tracing and logging by the following code:

```bash
git clone https://github.com/camel-ai/camel.git
cd camel
# Install camel

vim examples/debug/.traceroot-config.yaml
# Put your token to the file

python3 examples/debug/traceroot_example.py
```

### Explore the Loggings and Tracings with AI Agents

You can explore the loggings in the [TraceRoot.AI demo platform](https://test.traceroot.ai).

<img
  src="https://raw.githubusercontent.com/traceroot-ai/traceroot/refs/heads/main/misc/images/docs/quickstart/4_explore_logs.png"
  alt="Explore Logs"
  style={{ display: "block", margin: "0 auto" }}
/>

You can also show the code with some context by clicking the **Show Code** button.

<img
  src="https://raw.githubusercontent.com/traceroot-ai/traceroot/refs/heads/main/misc/images/docs/quickstart/5_explore_code.png"
  alt="Show Code"
  style={{ display: "block", margin: "0 auto" }}
/>

Next is the most exciting part, you can ask AI agents to help you on anything you want with your code, loggings, tracings and more.

Here is a simple example that ask to summarize the logs and corresponding code:

<img
  src="https://raw.githubusercontent.com/traceroot-ai/traceroot/refs/heads/main/misc/images/docs/quickstart/6_explore_chat.png"
  alt="Ask AI"
  style={{ display: "block", margin: "0 auto" }}
/>

At last, you can also check the latency for each of your functions by clicking the **Latency** button.

<img
  src="https://raw.githubusercontent.com/traceroot-ai/traceroot/refs/heads/main/misc/images/docs/quickstart/7_explore_latency.png"
  alt="Latency"
  style={{ display: "block", margin: "0 auto" }}
/>
