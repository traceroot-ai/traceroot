---
title: Bring Your Own Key (BYOK)
description: Use your own LLM API keys to power the TraceRoot AI agent
---

By default, TraceRoot provides system models for the AI agent. With BYOK, you can bring your own API keys to control which model is used.

## Default Models

{/* Mirrors the system model list in code, and a test keeps the two in sync.
    Models available only through BYOK do not belong here — see BYOK Models below. */}

| Provider | Models (first entry is the default) |
|----------|--------------------------------------|
| Anthropic | `claude-opus-4-8`, `claude-opus-4-7`, `claude-opus-4-6`, `claude-sonnet-5`, `claude-sonnet-4-6`, `claude-opus-4-5`, `claude-sonnet-4-5`, `claude-haiku-4-5` |
| OpenAI | `gpt-5.5`, `gpt-5.4`, `gpt-5.4-mini`, `gpt-5.4-nano`, `gpt-5`, `gpt-5-mini`, `o3`, `o4-mini`, `gpt-5.3-codex` |

See [Supported Models](/integrations/internal-models) for a description of each system model.

## Supported Providers

OpenAI, Anthropic, Azure OpenAI, Google (Gemini), Amazon Bedrock, xAI, DeepSeek, OpenRouter, Kimi, GLM.

## BYOK Models

Your own key unlocks models beyond the system list above — typically newer frontier
models, or models TraceRoot does not run on its own keys. The table below is generated
from the model catalog, so it stays current as providers ship new models.

{/* Generated from ADAPTER_MODELS in frontend/packages/core/src/llm-providers.ts — do not edit.
    Regenerate with: pnpm --filter @traceroot/core generate:byok-docs */}

<Accordion title="Models your own key adds">

| Provider | Models |
|----------|--------|
| OpenAI | `gpt-5.6-sol`, `gpt-5.6-terra`, `gpt-5.6-luna`, `gpt-5.5-pro`, `gpt-5.4-pro`, `gpt-5.2` |
| Anthropic | `claude-opus-5`, `claude-fable-5` |
| Google (Gemini) | `gemini-3.5-flash`, `gemini-3.1-pro-preview`, `gemini-3-flash-preview`, `gemini-3.1-flash-lite-preview`, `gemini-2.5-flash`, `gemini-2.5-pro` |
| DeepSeek | `deepseek-v4-pro`, `deepseek-v4-flash`, `deepseek-chat` |
| xAI | `grok-4.5`, `grok-4.20`, `grok-4` |
| Kimi | `kimi-k3`, `kimi-k2.6`, `kimi-k2.5`, `kimi-k2-thinking` |
| GLM | `glm-5.2`, `glm-5.1`, `glm-5`, `glm-5-turbo`, `glm-4.7`, `glm-4.6`, `glm-4.5`, `glm-4.5-air`, `glm-4.5-flash` |

</Accordion>

{/* end generated region */}

Providers not listed above accept a free-text model name instead of a dropdown. The model
selector in the AI chat interface is always the authoritative view of what your workspace
can use.

## Setup

1. Go to Workspace Settings -> Model Providers in the dashboard
2. Select a provider
3. Enter your API key
4. Optionally set a custom base URL for self-hosted or proxied models
5. Save — keys are encrypted at rest and scoped to your workspace

Once configured, the model selector in the AI chat interface lists both the system models above and every model your key makes available.
